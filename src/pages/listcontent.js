import React from "react";
import {
  useRouteMatch,
  BrowserRouter as Router,
  useParams
} from "react-router-dom";

export default function home() {
  // console.log("parms ", useParams());
  // console.log("Router ", useRouteMatch());
  return <div></div>;
}
